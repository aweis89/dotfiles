# Completions for gcloud-update-kubeconfig function
# Inherit completions from gcloud container clusters get-credentials
complete -c gcloud-update-kubeconfig --wraps 'gcloud container clusters get-credentials'
