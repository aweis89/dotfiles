#!/bin/bash

pr_num=$1

gh pr diff $pr_num | delta

read -r -p "Approve PR #$pr_num? [y/N] " reply
if [[ "$reply" =~ ^[Yy]([Ee][Ss])?$ ]]; then
  gh pr review "$pr_num" --approve
else
  echo "Skipped approval."
fi
