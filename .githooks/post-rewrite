#!/bin/sh
# Triggered after git rebase (and git pull --rebase).
# $1 indicates what caused the rewrite (rebase or amend); only act on rebase.
[ "$1" = "rebase" ] || exit 0
. "$(dirname "$0")/_chezmoi-apply.sh"
