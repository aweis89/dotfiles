#!/bin/bash

model="${1:-gemini:gemini-2.5-pro-preview-03-25}"

git diff --staged | aichat \
  --model "$model" \
  "Generate a concise conventional commit message for this change. Only respond with the commit message."
