#!/bin/bash

aichat \
  --model gemini:gemini-2.0-flash-lite \
  -f '`git diff --staged`' \
  "Respond with a single commit message for this change. Don't add any extra text as the output will be used directly."
